Backend for ORGAN-DPP Molecular Generator Studio

Run:

1. Create Python 3.10+ venv and install dependencies from `requirements.txt`.
2. Start: `uvicorn main:app --host 0.0.0.0 --port 8000 --reload`

Notes:
- RDKit installation may require conda; see Dockerfile for hints.
